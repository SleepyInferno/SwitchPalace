---
phase: 01-foundation-install-pipeline
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-26T19:00:00.000Z
---

<current_state>
Phase 01, Plan 03, Task 3 (hardware verification checkpoint). Four crash rounds diagnosed across four sessions. User needs to test the latest `dist/SwitchPalace.nro` (6.8 MB) on real Switch hardware.

This is the fourth build. All known crashes are fixed. New icon installed. Waiting on hardware test results.
</current_state>

<completed_work>

- Plan 01-01: Core install pipeline — Done
- Plan 01-02: NSP/XCI installers with already-installed detection — Done
- Plan 01-03: All UI views wired to install pipeline — Done
- fix(hw-1): USE_STD_THREAD=ON to fix pthread task loop crash (session 1, commit 5f15763)
- fix(hw-2): ScrollingFrame.setPadding removed — borealis NO_PADDING macro calls fatal() -> std::logic_error -> Instruction Abort (session 2, commit e092866)
- fix(hw-3): Install crash triple fix (session 3, uncommitted):
  1. Wrong hash check in nsp.cpp: memcmp(sha256_hash, content_id, 16) — NCA content IDs are not SHA-256 hashes
  2. Data race on m_installComplete — changed to std::atomic<bool>
  3. Repeated transitionToSummary — added m_hasTransitioned guard + m_updateTask->stop()
- **fix(hw-4): brls::sync() transition fix (this session, uncommitted):**
  1. **transitionToSummary deferred via brls::sync()**: Was called directly from UIUpdateTask::run() → updateUI(), i.e. from inside Ticking::updateTickings(). Calling pushActivity() from inside updateTickings() caused a Data Abort at X[0]=null (PC = SwitchPalace + 0x84cc0, borealis activity management). Fixed by wrapping the call in brls::sync([this]() { transitionToSummary(); }) so it runs in performSyncTasks() AFTER the current frame renders.
  2. **Double popActivity bug fixed in summary_view.cpp**: Two synchronous popActivity() calls both targeted SummaryView (deferred deletion means it wasn't removed from the stack yet). Fixed with callback chain: popActivity(FADE, []() { popActivity(); }).
  3. **New icon**: resources/icon.jpg replaced with Grand Nintendoh Palace 256x256 JPEG (center-cropped from user's 1024x559 source).
  4. **More granular logging added**: fbDebugLog around onInstallPressed pushActivity, ivLog for transition guard, sync queue, SummaryView creation, pushActivity call/return.
- **Docker rebuild:** Clean full build after all four fixes, dist/SwitchPalace.nro (6.8 MB)
</completed_work>

<remaining_work>

- Task 3: Await hardware test results from user
  - Watch `switchpalace_debug.log` for sequence:
    1. `onInstallPressed: creating InstallProgressView`
    2. `onInstallPressed: pushActivity InstallProgressView`
    3. `onInstallPressed: pushActivity returned`
    4. `install: starting install thread`
    5. `install: thread complete`
    6. `install: transition guard triggered`
    7. `install: queuing transitionToSummary via brls::sync`
    8. `install: transitionToSummary called`
    9. `install: creating SummaryView`
    10. `install: pushActivity SummaryView`
    11. `install: pushActivity returned`
  - Verify: progress bar advances, summary screen appears, B returns to file browser
  - If crash is fixed → verify full UAT checklist from 01-03-PLAN.md Task 3 (8 verification steps)
  - If new crash → analyze new Atmosphere log files, diagnose, fix, rebuild
  - On full pass:
    - Remove debug logging: `debugLog` calls in `source/main.cpp`, `fbDebugLog` in `source/ui/file_browser.cpp`, `ivLog` in `source/ui/install_view.cpp`
    - Write `01-03-SUMMARY.md` to complete Phase 01
</remaining_work>

<decisions_made>

- brls::sync() is the correct borealis pattern for UI transitions from Ticking callbacks — pushActivity must not be called from inside Ticking::updateTickings()
- Double popActivity must use callback chain, not two synchronous calls — deferred deletion leaves the activity on the stack until animation completes
- Wrong hash check removed from nsp.cpp: NCA content IDs are 128-bit identifiers, not SHA-256 hashes
- std::atomic<bool> required for m_installComplete — any cross-thread bool is a data race in C++
- m_hasTransitioned flag + m_updateTask->stop() prevents multiple sync() queue entries
- Prior session decisions still stand: nifmInitialize, deko3D backend, USE_STD_THREAD=ON, 64MB heap, padding on m_listContainer not m_scrollFrame
</decisions_made>

<blockers>

- None. Awaiting hardware test results.
</blockers>

<context>
Session 4 crash analysis:
- Atmosphere crash log 01774541335_0100b6200d8d2000.log: Data Abort at 0x0, PC = SwitchPalace + 0x84cc0 (shifted +0xC0 from session 3 crash due to code size change), X[0]=null, ReturnAddress[04]=0x1dca4 (our code), ReturnAddress[05]=0x1da60 (our code)
- Call chain: our code (0x1da60 = updateUI?) → our code (0x1dca4 = transitionToSummary?) → borealis pushActivity (0x84cc0) → crash
- Root cause: pushActivity called from inside Ticking::updateTickings() (steps: processInput → updateTickings → frame → performSyncTasks). The fix defers it to performSyncTasks via brls::sync().
- Session 3 fixes (m_hasTransitioned guard) were correct but insufficient — the guard prevented multiple pushActivity calls but didn't fix the timing issue of calling pushActivity from updateTickings.

Uncommitted files: source/ui/install_view.cpp, source/ui/summary_view.cpp, source/ui/file_browser.cpp, resources/icon.jpg, dist/SwitchPalace.nro
</context>

<next_action>
Start with: User returns with hardware test results. Read switchpalace_debug.log and check which of the 11 log lines appeared. If crash: analyze new Atmosphere logs. If full pass: remove debug logging and write 01-03-SUMMARY.md.
</next_action>
