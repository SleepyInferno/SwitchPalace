---
phase: 01-foundation-install-pipeline
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-26T14:44:57.873Z
---

<current_state>
Phase 01, Plan 03, Task 3 (hardware verification checkpoint). User is actively testing the freshly built NRO on real Switch hardware RIGHT NOW and will return with crash logs or a pass/fail report.

The NRO at `dist/SwitchPalace.nro` (6.5 MB) was just rebuilt via Docker after fixing a second crash bug found via log analysis in this session.
</current_state>

<completed_work>

- Plan 01-01: Core install pipeline — Done
- Plan 01-02: NSP/XCI installers with already-installed detection — Done
- Plan 01-03: All UI views wired to install pipeline — Done
- fix(hw): USE_STD_THREAD=ON to fix pthread task loop crash (prior session, commit 5f15763)
- **Crash fix (this session):** Second crash diagnosed from new Atmosphère logs + SD debug log:
  - `brls::ScrollingFrame::setPadding()` calls `fatal()` → throws `std::logic_error` — borealis explicitly forbids padding on ScrollingFrame (NO_PADDING macro in scrolling_frame.cpp:582)
  - Uncaught exception caused Instruction Abort at `0x462e993368`; all regs X00–X17 zeroed (unhandled throw pattern)
  - **Fix:** Removed `m_scrollFrame->setPadding(8,32,8,32)` from `source/ui/file_browser.cpp`, moved padding to `m_listContainer->setPadding(8,32,8,32)` (the content view — correct per borealis API)
- **Icon created:** `resources/icon.jpg` (256×256 JPEG, castle motif, Royal Sapphire #3D9CF0 palette)
- **CMakeLists.txt updated:** Added `--icon=${CMAKE_SOURCE_DIR}/resources/icon.jpg` to `elf2nro` command
- **Docker rebuild:** Clean full build, `dist/SwitchPalace.nro` refreshed
- Task 3: Hardware verification — **IN PROGRESS** (user testing now)
</completed_work>

<remaining_work>

- Task 3: Await hardware test results from user
  - If crash is fixed → verify full UAT checklist from 01-03-PLAN.md Task 3 (8 verification steps)
  - If new crash → analyze new log files, diagnose, fix, rebuild
  - On full pass:
    - Remove debug logging: `debugLog` calls in `source/main.cpp` and `fbDebugLog` calls in `source/ui/file_browser.cpp`
    - Write `01-03-SUMMARY.md` to complete Phase 01
</remaining_work>

<decisions_made>

- Padding moved to `m_listContainer` (content view) rather than wrapping in another Box — minimal layout, correct borealis usage
- Icon generated programmatically with PIL — castle motif matches design system (Royal Sapphire)
- `dist/SwitchPalace.nro` is canonical deploy artifact
- hbmenu crash in `01774534995` (null deref in `folder_icon.bin`) is hbmenu-internal, not ours — no action
- Prior session decisions still stand: nifmInitialize in switch_wrapper.c, deko3D backend, USE_STD_THREAD=ON, 64MB heap
</decisions_made>

<blockers>

- None currently. Awaiting hardware test results.
</blockers>

<context>
Three Atmosphère crash logs analyzed this session:
1. `switchpalace_debug.log` — SD card log showed 6 boot cycles; last two died after "scrollFrame: setPadding" log line
2. `01774535048_0100b6200d8d2000.log` — NRO Instruction Abort; zeroed regs = unhandled exception throw, not vtable corruption
3. `01774534995_010000000000100d.log` — hbmenu null deref in folder_icon.bin (unrelated hbmenu bug)

Borealis source confirmed: `scrolling_frame.cpp:582` defines `NO_PADDING` macro that calls `fatal()` → `throw std::logic_error`. Any call to `setPadding` on a ScrollingFrame is fatal. Fix is a 1-line change.

Debug logging (fbDebugLog in file_browser.cpp) should be removed once hardware confirms working — it adds SD card write overhead on every boot.
</context>

<next_action>
Start with: Review new log files from user's hardware test. If no crash, walk through the 8-step UAT checklist from 01-03-PLAN.md Task 3 `how-to-verify` section. If crash, analyze logs and fix. On full pass: remove debug logging, write 01-03-SUMMARY.md.
</next_action>
