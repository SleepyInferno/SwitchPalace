---
phase: 01-foundation-install-pipeline
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-26T17:07:35.467Z
---

<current_state>
Phase 01, Plan 03, Task 3 (hardware verification checkpoint). Four crash rounds diagnosed across four sessions. Latest build `dist/SwitchPalace.nro` (6.8 MB) is ready for hardware testing. User is about to test.

**WORKFLOW NOTE FOR NEXT SESSION:** User has requested that crash debugging be split between Claude and Codex (OpenAI Codex CLI) to reduce context load. On resume: confirm with user whether they mean (a) OpenAI Codex CLI running separately in parallel, or (b) Claude Code agent subagents for research delegation. Implement whichever they confirm before starting any new debug round.
</current_state>

<completed_work>

- Plan 01-01: Core install pipeline — Done
- Plan 01-02: NSP/XCI installers with already-installed detection — Done
- Plan 01-03: All UI views wired to install pipeline — Done
- fix(hw-1): USE_STD_THREAD=ON — pthread task loop crash (commit 5f15763)
- fix(hw-2): ScrollingFrame.setPadding removed — borealis NO_PADDING → fatal() → std::logic_error → Instruction Abort (commit e092866)
- fix(hw-3): Triple fix: wrong hash check in nsp.cpp, atomic bool data race, m_hasTransitioned guard (commit, see context)
- fix(hw-4): brls::sync() transition fix + double popActivity fix + new icon (commit 280efdb):
  1. `transitionToSummary()` now deferred via `brls::sync()` — was called from inside `Ticking::updateTickings()`, which runs before `Application::frame()`. Calling `pushActivity` from that context caused Data Abort at X[0]=null (PC = SwitchPalace + 0x84cc0).
  2. Double `popActivity()` in summary_view.cpp fixed with callback chain.
  3. New icon: resources/icon.jpg — Grand Nintendoh Palace 256x256 JPEG.
  4. 11-checkpoint logging added (onInstallPressed through pushActivity returned).
</completed_work>

<remaining_work>

- Task 3: Hardware test (user testing now)
  - Watch `switchpalace_debug.log` for all 11 checkpoints in order:
    1. `onInstallPressed: creating InstallProgressView`
    2. `onInstallPressed: pushActivity InstallProgressView`
    3. `onInstallPressed: pushActivity returned`
    4. `install: starting install thread`
    5. `install: thread complete`
    6. `install: transition guard triggered`
    7. `install: queuing transitionToSummary via brls::sync`
    8. `install: transitionToSummary called`
    9. `install: creating SummaryView`
    10. `install: pushActivity SummaryView`
    11. `install: pushActivity returned`
  - On full pass:
    - Remove debug logging (fbDebugLog in file_browser.cpp, ivLog in install_view.cpp, debugLog in main.cpp)
    - Write 01-03-SUMMARY.md to complete Phase 01
  - On crash: read new Atmosphere log + debug log, identify last checkpoint reached, diagnose from there
</remaining_work>

<decisions_made>

- brls::sync() is required for any pushActivity call that originates from a Ticking/RepeatingTask callback
- Double popActivity must use callback chain (deferred deletion keeps activity on stack until animation ends)
- Wrong hash check removed from nsp.cpp: NCA content IDs are 128-bit identifiers, not SHA-256 hashes
- std::atomic<bool> for m_installComplete (cross-thread bool is UB)
- m_hasTransitioned + m_updateTask->stop() prevents queuing multiple sync() calls
- All prior session decisions stand: nifmInitialize, deko3D backend, USE_STD_THREAD=ON, 64MB heap
</decisions_made>

<blockers>

- Awaiting hardware test results
- WORKFLOW SETUP NEEDED: Before next debug round, confirm and implement Codex+Claude split workflow with user
</blockers>

<context>
Crash history:
- Session 1 (commit 5f15763): pthread task loop crash → USE_STD_THREAD=ON
- Session 2 (commit e092866): ScrollingFrame.setPadding → NO_PADDING macro → fatal() → std::logic_error → Instruction Abort
- Session 3 (uncommitted at pause): Data Abort at 0x84c00, X[0]=null — wrong hash check killed every install immediately, causing instant m_installComplete=true, UIUpdateTask fired every 100ms pushing unlimited activities → null deref in borealis activity stack
- Session 4 (commit 280efdb): Same crash pattern (0x84cc0, shifted +0xC0 from session 3 due to code size change). Session 3 guard was correct but insufficient. Root cause: pushActivity called from updateTickings() (before frame()). Fix: brls::sync() defers to performSyncTasks() (after frame()).

Debug approach that ran context high: reading 15+ borealis source files one-by-one in main context to trace execution paths. Next session: delegate source file research to Explore subagent, only receive findings summary.
</context>

<next_action>
1. FIRST: Ask user to clarify Codex intent — OpenAI Codex CLI (external, parallel) or Claude Code agent subagents (internal delegation). Set up accordingly.
2. Receive hardware test results (log files + pass/fail report).
3. If crash: identify last checkpoint in debug log, read only the Atmosphere log in main context, delegate source-file research to subagent with specific question, apply fix.
4. If pass: remove debug logging, write 01-03-SUMMARY.md.
</next_action>
