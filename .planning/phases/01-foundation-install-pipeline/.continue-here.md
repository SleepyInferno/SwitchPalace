---
phase: 01-foundation-install-pipeline
task: hw-verify (blocked on crash)
total_tasks: 3 plans + hw-verify
status: blocked
last_updated: 2026-03-26T12:39:10.958Z
---

<current_state>
Plans 01-01, 01-02, 01-03 all implemented and built. Stuck at hw-verify checkpoint:
app crashes on real Switch hw (fw 21.1.0, Atmosphere 1.10.1) with 2168-0002 (data abort)
during deko3D initialization. Debug NRO is built with SD card logging to identify the exact step.
</current_state>

<completed_work>

- Plan 01-01: Core install pipeline - Done (commit 777a7a7)
- Plan 01-02: NSP/XCI installers with already-installed detection - Done (commit f04cbf7)
- Plan 01-03: All UI views wired to install pipeline - Done (commit 584174d)
- fix(hw) commit b91c067: Added nifmInitialize — borealis calls nifm for status bar every frame; uninitialized = IPC abort = 2168-0001
- fix(hw) commit 6567318: Switched from GLFW+EGL to deko3D — GLFW glfwInit fails in applet mode on fw 21.1.0; deko3D is the correct Switch GPU API. NRO: 12MB -> 6.5MB
- debug commit f8c22a4: Added sdmc:/switchpalace_debug.log with per-step checkpoints inside SwitchVideoContext ctor and main.cpp
</completed_work>

<remaining_work>

- BLOCKER: User needs to run debug NRO, retrieve sdmc:/switchpalace_debug.log and sd:/atmosphere/crash_reports/*.log
- Fix whichever deko3D step the log shows as last before crash
- Remove debug logging from switch_video.cpp and main.cpp
- Rebuild final NRO and hw-verify
- Write 01-03-SUMMARY.md
</remaining_work>

<decisions_made>

- nifmInitialize(NifmServiceType_User) added to switch_wrapper.c — borealis status bar calls nifm functions directly; TRUST-03 maintained (no socketInit)
- Video backend: GLFW+EGL -> deko3D (-DUSE_DEKO3D=ON in Dockerfile + Makefile). resources/shaders/ created for compiled .dksh shaders
- __nx_heap_size: 128MB -> 64MB (128MB = hbloader ceiling with no headroom in applet mode)
- try/catch wraps brls::Application::init()+createWindow() so borealis fatal() throws produce EXIT_FAILURE not abort
- Build is Docker-only (docker build -t switchpalace-build . && docker cp switchpalace-extract:/build/build/SwitchPalace.nro ./dist/SwitchPalace.nro)
</decisions_made>

<blockers>
- 2168-0002 data abort during deko3D init: exact step unknown; debug NRO will reveal it via sdmc:/switchpalace_debug.log
- Possible causes: DeviceMaker::create, nwindowGetDefault() null, renderer::emplace shader load fail, nvgCreateDk null
</blockers>

<context>
Three crashes fixed so far in sequence: nifm uninitialized -> GLFW applet mode fail -> current deko3D data abort.
Each fix has changed the error code and crash address, confirming progress. The debug logging added to
borealis/switch_video.cpp SwitchVideoContext constructor will pinpoint exactly which of the ~8 deko3D
init steps crashes. Once known, the fix is targeted. Do NOT start adding features until crash is resolved.

Build system: Docker (devkitpro/devkita64:latest has full portlibs). Local devkitPro works for deko3D
but not GLFW — the deko3D switch was the right call regardless.

DBI is closed-source; Goldleaf is the reference for service init patterns.
Atmosphere crash reports are at sd:/atmosphere/crash_reports/ and include FAR (fault address).
</context>

<next_action>
Start with: Ask user for content of sdmc:/switchpalace_debug.log and the newest file in sd:/atmosphere/crash_reports/.
The last line of the debug log = exact deko3D step that crashed.
Fix that step, remove all debug logging (dkDebugLog in switch_video.cpp, debugLog in main.cpp), rebuild, hw-verify.
</next_action>
