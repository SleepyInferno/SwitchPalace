---
phase: 01-foundation-install-pipeline
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-03-26T17:36:55.505Z
---

<current_state>
Phase 01, Plan 03, Task 4 (hw-verify fix-5). Five crash rounds total. Latest fix (fix-5) is in source/ui/file_browser.cpp — NOT YET BUILT. Need docker rebuild → deploy → test.

**fix-5 root cause:** buildFileList() calls clearViews() which frees focused rows to deletionPool, but Application::currentFocus is NOT updated. Old row deleted at end-of-frame → dangling pointer. Next frame: vtable ptr in freed memory = 0 → virtual dispatch at vtable[0xf0] → Data Abort at address 0xf0.

**fix-5 change:** Added `brls::Application::giveFocus(getDefaultFocus())` after buildFileList() in:
- Click action handler (file_browser.cpp ~line 354)
- B button deselect handler (file_browser.cpp ~line 142)

**Codex workflow confirmed:** Option B — internal Explore subagents for borealis source research. DO NOT read 15+ borealis source files in main context.
</current_state>

<completed_work>

- Plan 01-01: Core install pipeline — Done (commit 777a7a7)
- Plan 01-02: NSP/XCI installers with already-installed detection — Done (commit f04cbf7)
- Plan 01-03: All UI views wired to install pipeline — Done (commit 584174d)
- fix(hw-1): USE_STD_THREAD=ON — pthread task loop crash (commit 5f15763)
- fix(hw-2): ScrollingFrame.setPadding removed — borealis NO_PADDING fatal() crash (commit e092866)
- fix(hw-3): Triple fix — wrong hash check nsp.cpp, atomic bool data race, m_hasTransitioned guard (commit 3375248)
- fix(hw-4): brls::sync() transition fix + double popActivity fix + new icon (commit 280efdb)
- fix(hw-5): dangling currentFocus after buildFileList() — giveFocus fix (UNCOMMITTED, pending build)
</completed_work>

<remaining_work>

- Task 4: hw-verify fix-5 (build, deploy, test file selection)
  - Build: docker run CMake rebuild → dist/SwitchPalace.nro
  - Test: launch app, select a file with A button (should rebuild list without crash)
  - Test: press B with files selected (deselects, rebuilds list — should not crash)
  - Check switchpalace_debug.log for normal operation
  - On pass → proceed to task 5
  - On crash → read Atmosphere log, spawn Explore subagent with specific question

- Task 5: Remove debug logging after confirmed working
  - fbDebugLog calls in source/ui/file_browser.cpp
  - ivLog calls in source/ui/install_view.cpp
  - debugLog calls in source/main.cpp

- Task 6: Write 01-03-SUMMARY.md to complete Phase 01
</remaining_work>

<decisions_made>

- Root cause of session-5 crash: dangling Application::currentFocus after buildFileList() clears rows
  - clearViews() frees rows to deletionPool (not immediately deleted)
  - Application::currentFocus still points to old row
  - Deletion pool flushed at end of frame → currentFocus is dangling
  - Next frame: vtable ptr at freed memory = 0 → vtable[0xf0] → fault address 0xf0
  - X[6]='_opacity' in crash = bytes 6-13 of "brls/highlight/shadow_opacity" key in drawHighlight
- Fix: giveFocus(getDefaultFocus()) after every buildFileList() call from action handlers
- Codex = Option B confirmed: Claude Explore subagents for borealis research, NOT OpenAI Codex CLI
- All prior session decisions stand (nifmInitialize, deko3D, USE_STD_THREAD=ON, 64MB heap, brls::sync)
</decisions_made>

<blockers>

- Awaiting hardware test of fix-5 build
</blockers>

<context>
Crash history:
- Session 1 (commit 5f15763): pthread task loop crash → USE_STD_THREAD=ON
- Session 2 (commit e092866): ScrollingFrame.setPadding → NO_PADDING → fatal() → std::logic_error → Instruction Abort
- Session 3 (commit 3375248): wrong hash check killed every install immediately; data race on m_installComplete; repeated transitionToSummary
- Session 4 (commit 280efdb): pushActivity from updateTickings() (before frame()) → Data Abort X[0]=null; double popActivity in summary_view
- Session 5 (fix-5 pending): dangling currentFocus after buildFileList() → Data Abort vtable[0xf0]=null

Session 5 debug approach: spawned 2 parallel Explore subagents (borealis rendering path + SwitchPalace source). Both returned findings. Main context analyzed crash log directly. Total analysis time: ~25min.

For any future crash: spawn Explore subagent with Atmosphere log + specific hypothesis question. Never read borealis sources directly in main context.
</context>

<next_action>
1. Build: docker rebuild → dist/SwitchPalace.nro
2. Deploy to SD card
3. hw-verify: launch app → select a file with A → should NOT crash → press B to deselect → should NOT crash
4. If pass: remove debug logging, write 01-03-SUMMARY.md
5. If crash: provide new Atmosphere log + debug log here
</next_action>
