---
phase: 01-foundation-install-pipeline
task: hw-verify (blocked on crash)
total_tasks: 3 plans + hw-verify
status: blocked
last_updated: 2026-03-26T13:14:36.197Z
---

<current_state>
Plans 01-01, 01-02, 01-03 all implemented and built. Stuck at hw-verify checkpoint.
App crashes on real Switch hw (fw 21.1.0, Atmosphere 1.10.1) during deko3D/borealis init.
Second round of SD card debug logging added to main.cpp and file_browser.cpp constructor.
Debug NRO is at dist/SwitchPalace.nro — needs hardware test to get the log output.
</current_state>

<completed_work>

- Plan 01-01: Core install pipeline - Done (commit 777a7a7)
- Plan 01-02: NSP/XCI installers with already-installed detection - Done (commit f04cbf7)
- Plan 01-03: All UI views wired to install pipeline - Done (commit 584174d)
- fix(hw) b91c067: Added nifmInitialize — borealis calls nifm for status bar every frame; uninitialized = IPC abort = 2168-0001
- fix(hw) 6567318: Switched from GLFW+EGL to deko3D — GLFW glfwInit fails in applet mode on fw 21.1.0. NRO: 12MB -> 6.5MB
- debug f8c22a4: Added sdmc:/switchpalace_debug.log with per-step checkpoints in main.cpp
- debug 41736e5: Added second round of logging into file_browser.cpp FileBrowserView ctor
</completed_work>

<remaining_work>

- BLOCKER: User needs to deploy dist/SwitchPalace.nro, launch on hardware, retrieve sdmc:/switchpalace_debug.log
- Fix whichever step is last in the log before crash
- Remove ALL debug logging (debugLog in main.cpp, fbDebugLog in file_browser.cpp)
- Rebuild final NRO and run full hw-verify checklist from 01-03-PLAN.md Task 3
- Write 01-03-SUMMARY.md to complete Phase 01
</remaining_work>

<decisions_made>

- nifmInitialize(NifmServiceType_User) added to switch_wrapper.c — borealis status bar calls nifm; TRUST-03 maintained (no socketInit)
- Video backend: GLFW+EGL -> deko3D (-DUSE_DEKO3D=ON). resources/shaders/ created for compiled .dksh shaders
- __nx_heap_size: 64MB (0x4000000) — safe ceiling for both applet and application mode
- try/catch wraps brls::Application::init()+createWindow() so borealis fatal() produces EXIT_FAILURE not abort
- Build is Docker-only: `docker build -t switchpalace-build . && docker cp switchpalace-extract:/build/build/SwitchPalace.nro ./dist/SwitchPalace.nro`
</decisions_made>

<blockers>
- Crash during deko3D/borealis init: exact step unknown; second debug round added. Log covers:
  "main: entered" → "mode: APPLET/APPLICATION" → "borealis: calling Application::init" →
  "borealis: Application::init OK, calling createWindow" → "borealis: createWindow OK" →
  "cleanup: entering ContentManager cleanup" → "cleanup: ContentManager cleanup done" →
  "ui: constructing FileBrowserView" → "FileBrowserView: ctor start" →
  "FileBrowserView: base layout set" → "FileBrowserView: titleBar added"
  Last line in log = crash step.
</blockers>

<context>
Three crashes fixed in sequence: nifm uninitialized -> GLFW applet mode fail -> current unknown step.
Each fix changed the error code and crash address, confirming progress.

Atmosphere crash reports are at sd:/atmosphere/crash_reports/ and include fault address (FAR) — useful
alongside the debug log.

Do NOT add features or refactor until the crash is resolved and hw-verify passes.
DBI is closed-source; Goldleaf is the reference for service init patterns.
</context>

<next_action>
Start with: Ask user for content of sdmc:/switchpalace_debug.log after deploying dist/SwitchPalace.nro.
The last line = exact crash step. Fix that step, remove all debugLog/fbDebugLog calls, rebuild, then
run the full hw-verify checklist from 01-03-PLAN.md Task 3 how-to-verify section.
</next_action>
