---
phase: 01-foundation-install-pipeline
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-26T18:00:00.000Z
---

<current_state>
Phase 01, Plan 03, Task 3 (hardware verification checkpoint). Three crash rounds have been diagnosed and fixed across three sessions. User needs to test the latest `dist/SwitchPalace.nro` (6.5 MB) on real Switch hardware.

This is the third build. All known crashes are fixed. Waiting on hardware test results.
</current_state>

<completed_work>

- Plan 01-01: Core install pipeline — Done
- Plan 01-02: NSP/XCI installers with already-installed detection — Done
- Plan 01-03: All UI views wired to install pipeline — Done
- fix(hw-1): USE_STD_THREAD=ON to fix pthread task loop crash (session 1, commit 5f15763)
- fix(hw-2): ScrollingFrame.setPadding removed — borealis NO_PADDING macro calls fatal() -> std::logic_error -> Instruction Abort (session 2, commit e092866)
- **fix(hw-3): Install crash triple fix (this session, uncommitted):**
  1. **Wrong hash check in `source/install/nsp.cpp`**: `memcmp(sha256_hash, content_id, 16)` failed every install immediately — NCA content IDs are NOT SHA-256 hashes. Removed the comparison; NCM verifies integrity internally.
  2. **Data race on `m_installComplete` in `include/ui/install_view.hpp`**: Changed plain `bool` → `std::atomic<bool> m_installComplete{false}`. Install thread writes, main thread reads — was UB.
  3. **Repeated `transitionToSummary()` in `source/ui/install_view.cpp`**: Without a guard, UIUpdateTask fired every 100ms after install completed, pushing unlimited `SummaryView` activities onto borealis stack → null pointer crash in borealis activity management (~43% offset in 1.9MB binary, PC = SwitchPalace + 0x84c00, X[0]=0x0). Fixed by adding `m_hasTransitioned` flag and calling `m_updateTask->stop()` before transitioning.
- **Docker rebuild:** Clean full build after all three fixes, `dist/SwitchPalace.nro` refreshed
</completed_work>

<remaining_work>

- Task 3: Await hardware test results from user
  - Watch `switchpalace_debug.log` for sequence:
    1. `install: starting install thread`
    2. `install: thread complete`
    3. `install: transitionToSummary called` — must appear **exactly once**
  - Verify: progress bar advances, summary screen appears, B returns to file browser
  - If crash is fixed → verify full UAT checklist from 01-03-PLAN.md Task 3 (8 verification steps)
  - If new crash → analyze new Atmosphere log files, diagnose, fix, rebuild
  - On full pass:
    - Remove debug logging: `debugLog` calls in `source/main.cpp`, `fbDebugLog` in `source/ui/file_browser.cpp`, `ivLog` in `source/ui/install_view.cpp`
    - Write `01-03-SUMMARY.md` to complete Phase 01
</remaining_work>

<decisions_made>

- Wrong hash check removed from nsp.cpp: NCA content IDs are 128-bit identifiers, not SHA-256 hashes; verifying them against a SHA-256 hash is always wrong
- `std::atomic<bool>` required for `m_installComplete` — any cross-thread bool is a data race in C++
- `m_hasTransitioned` flag + `m_updateTask->stop()` is the correct borealis pattern for one-shot transitions from a RepeatingTask context
- Debug logging (ivLog in install_view.cpp) is useful for verifying exact-once transition — keep until hardware confirmed
- Prior session decisions still stand: nifmInitialize, deko3D backend, USE_STD_THREAD=ON, 64MB heap, padding on m_listContainer not m_scrollFrame
</decisions_made>

<blockers>

- None. Awaiting hardware test results.
</blockers>

<context>
Crash analysis methodology for this session:
- Atmosphere crash log `01774537727_0100b6200d8d2000.log`: Data Abort at 0x0 (null ptr), main thread PC = SwitchPalace + 0x84c00 (~43% of 1.9MB binary = borealis code range, not app code), two threads active (install thread running at + 0x152384), X[0]=0x0 (null this/first arg to borealis method), TLS shows completed NCM IPC = install finished cleanly before crash
- Hypothesis: install completes fast (wrong hash check kills it immediately), m_installComplete=true, UIUpdateTask keeps firing, brls::Application::pushActivity called repeatedly, borealis internal list hits null ptr
- Confirmed fix: (1) make install succeed by removing wrong hash check, (2) prevent repeated pushActivity with m_hasTransitioned guard

Uncommitted files: source/ui/install_view.cpp, include/ui/install_view.hpp, source/install/nsp.cpp, dist/SwitchPalace.nro
</context>

<next_action>
Start with: User returns with hardware test results. Read new crash log files if any. If no crash, verify the 8-step UAT checklist. On full pass, remove all debug logging and write 01-03-SUMMARY.md.
</next_action>
